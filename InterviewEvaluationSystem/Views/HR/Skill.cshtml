@model IEnumerable<InterviewEvaluationSystem.Models.tblSkill> 

@{
    ViewBag.Title = "Skill";
    WebGrid grid = new WebGrid(Model);
}

<h2>Skill</h2>

@grid.GetHtml(tableStyle: "table table-bordered table-responsive table-hover",
    columns: new[]

{
    grid.Column("SkillName"),
    grid.Column("SkillCategoryID")
}
)


