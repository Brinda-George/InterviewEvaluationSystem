@model InterviewEvaluationSystem.Models.tblSkill

@{
    ViewBag.Title = "Skill";
   // Layout = "~/Views/Shared/_Layout.cshtml";
    //Layout = "~/Views/Shared/HRLayout.cshtml";
    //List<SelectListItem> selectedlist = ViewData["category"] as List<SelectListItem>;
}

<h2>Skill</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div ID="form1"  class="form-horizontal">
       
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <div class="form-group">
            @Html.LabelFor(model => model.SkillCategoryID, "SkillCategory", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("category", (IEnumerable<SelectListItem>)ViewBag.category, "Select category", new { id = "categories" })
                @*@Html.DropDownList("category",
                   (IEnumerable<SelectListItem>)ViewBag.category,
                   "Select category")*@
                @Html.ValidationMessageFor(model => model.SkillCategoryID, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.SkillName, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.SkillName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.SkillName, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" id="btnSubmit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}


<div id="ajaxgrid">
    @{
        var gridview = new WebGrid(ViewBag.Skillcategories,canSort:true,ajaxUpdateContainerId: "ajaxgrid", rowsPerPage: 5);
    }

    @gridview.GetHtml(
       
        tableStyle: "webgrid-table",
        headerStyle: "webgrid-header",
        footerStyle: "webgrid-footer",
        selectedRowStyle: "webgrid-selected-row",
        rowStyle: "webgrid-row-style",
        mode: WebGridPagerModes.All,
    columns: new[]

{
    
    @*gridview.Column("S.No",
        format:
        @<text>
            <span id="skillid" >@item.skillno</span>
        </text>),*@

    gridview.Column("Skill Type",
        format:
        @<text>
            <span id="skillcategory" >@item.skillcat</span>
        </text>),

    gridview.Column("Skill Name",
         format:
        @<text>
            <span id="skillname" class="read">@item.skillname</span>
            @Html.TextBox("SkillName", (string)item.skillname, new { @class = "edit",@style="display:none" })
        </text>),

    gridview.Column("Action", style: "button",
                format:
                @<text>
                    <button class="edit-case read" id="@item.skillno">Edit</button>
                    <button class="delete-case read" id="@item.skillno">Delete</button>
                    <button class="update-case edit" style="display:none" id="@item.skillno">Update</button>
                    <button class="cancel-case edit" style="display:none" id="@item.skillno">Cancel</button>
                </text>)
    }
)
</div>

<br />
<br />
@Html.ActionLink("Go back to home ?", "HRHomePage", "HR", new { @class = "elements" }, null)

<style type="text/css">

    #form1 {
        font-family: "Times New Roman", Arial, Helvetica, sans-serif;
        font-size: 1.2em;
        color: dodgerblue !important;
    }

    .webgrid-table {
        font-family: "Times New Roman", Arial, Helvetica, sans-serif;
        font-size: 1.2em;
        width: 100%;
        display: table;
        border-collapse: separate;
        border: solid 1px dodgerblue;
        background-color: white;
    }

        .webgrid-table td, th {
            border: 1px solid dodgerblue;
            padding: 3px 7px 2px;
        }

    .webgrid-header {
        background-color: black;
        color: black !important;
        padding-bottom: 4px;
        padding-top: 5px;
        text-align: left;
    }

    .webgrid-footer {
    }

    .webgrid-row-style {
        padding: 3px 7px 2px;
    }
</style>


<script type="text/javascript">
    $(document).ready(function () {
        $("[id*=btnSubmit]").click(function () {
            // var skillcategory = $("[id*=categories]");
            var skillcategory = document.getElementById('categories').value;
            if (skillcategory == null) {
                alert("Please select a category!");
                return false;
            }
            return true;
        });
    });
</script>


<script src="~/Scripts/jquery-1.10.2.js"></script>
<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.js"></script>
<script src="~/Scripts/Askill.js"></script>



